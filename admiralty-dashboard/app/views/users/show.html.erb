<h1>Admiral <%= @user.name %>'s Dashboard</h1>
<h2>Currently Active Captains:</h2>
<ul>
<% @captains.each do |c| %>
    <li><%= link_to c.name, captain_path(c.id) %> - currently commanding the <%= link_to c.current_ship.name, ship_path(c.current_ship.id) %></li>
<% end %>
</ul><br>
<%= link_to "Create New Captain", new_captain_path %>
