<h2>Current Captain Roster</h2>
<ul>
    <% @captains.each do |c| %>
        <li><%= link_to c.name, captain_path(c.id) %></li>
    <% end %>
</ul><br>
<%= link_to "Create New Captain", new_captain_path %>