<% if @captain %>
    Action involving Captain: <%= link_to @captain.name, captain_path(@captain.id) %><br>
<% end %>
<%= @action.action_type %> - involving the <%= link_to @ship.name, ship_path(@ship.id) %>